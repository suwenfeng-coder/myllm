# myllm 项目代码结构与待办优化方案

> 最后更新：2026-07-05（完成最新代码结构复盘与待优化项归档）

---

## 零、当前代码结构分析

### 0.1 技术栈

| 层级 | 技术 |
|------|------|
| 框架 | Spring Boot 3.4.1、Java 17、Spring AI 1.0.0 |
| 对话 | Spring AI + Ollama（`qwen3:8b`）/ OpenAI 兼容 |
| 向量 | Spring AI Embedding + Ollama（`bge-m3`，1024 维） |
| 业务库 | MySQL（JPA）— 对话日志、RAG 日志、清洗/存储审计、上传/构图任务 |
| 向量库 | PostgreSQL + pgvector + ParadeDB pg_search（JdbcTemplate）— Dense + BM25 |
| 对象存储 | MinIO — 原文件与解析后 Markdown |
| 图数据库 | Neo4j 5.x（可选）— Document/Section/Chunk/Entity 结构图 |
| 文档解析 | Apache POI + DocForge（Docling/Maker 远程）+ 本地多格式解析 |
| 前端 | 静态页 `static/index.html`（对话 + 异步上传 + RAG + 文件管理）与 `static/eval.html`（模型评测） |

### 0.2 目录结构

```text
myllm/
├── pom.xml
├── src/main/
│   ├── java/com/example/myllm/
│   │   ├── MyllmApplication.java          # 启动入口
│   │   ├── config/                        # 配置层
│   │   │   ├── LlmConfig.java             # ChatClient Bean（Ollama/OpenAI）
│   │   │   ├── VectorDataSourceConfig.java # pgvector 独立 JdbcTemplate
│   │   │   └── GlobalExceptionHandler.java # 全局异常（文本响应）
│   │   ├── controller/                    # 核心 REST API（Chat/File/Graph/Log）
│   │   │   ├── ChatController.java        # /api/chat
│   │   │   ├── FileEmbeddingController.java # /api/files/*
│   │   │   ├── GraphController.java       # /api/graph/*
│   │   │   └── RequestLogController.java  # /api/logs/requests
│   │   ├── service/                         # 业务服务（对话、RAG、入库、构图、脚本状态）
│   │   │   ├── ChatService.java             # 对话 + RAG 编排
│   │   │   ├── FileEmbeddingService.java    # 上传→清理→分块→向量→检索
│   │   │   ├── RagRetrievalService.java     # 混合检索编排
│   │   │   ├── Bm25RetrievalService.java    # ParadeDB BM25
│   │   │   ├── DocumentParseService.java    # auto/local/docling/maker 路由
│   │   │   ├── DocumentCleaningService.java # 保守型数据清理
│   │   │   ├── DocumentChunkingService.java # 分块策略调度
│   │   │   ├── DocumentStorageService.java  # MinIO 原文件与解析结果
│   │   │   ├── FileUploadTaskService.java   # 异步上传任务
│   │   │   ├── FileUploadTaskWorker.java    # 上传任务消费者
│   │   │   ├── GraphIndexingService.java    # Neo4j 结构图写入
│   │   │   ├── GraphIndexTaskService.java   # 构图任务管理
│   │   │   ├── GraphIndexTaskWorker.java    # 构图任务消费者
│   │   │   ├── TransactionLogService.java   # 对话交易日志
│   │   │   ├── RagCallLogService.java       # RAG 调用日志
│   │   │   └── RequestLogQueryService.java  # 日志联查
│   │   ├── entity/                          # JPA 实体（MySQL）
│   │   │   ├── TransactionLog.java
│   │   │   ├── RagCallLog.java
│   │   │   ├── DocumentCleaningLog.java
│   │   │   ├── DocumentStorageLog.java
│   │   │   ├── DocumentUploadTask.java
│   │   │   └── DocumentGraphIndexTask.java
│   │   ├── repository/                      # Spring Data JPA
│   │   ├── dto/                             # 请求/响应对象
│   │   ├── eval/                            # 本地模型评测 API、任务、评分与报告
│   │   ├── harness/                         # Harness 控制平面（Run/Step/Tool/Event/Verification）
│   │   └── support/                         # 工具与算法
│   │       ├── FileTextExtractor.java       # 多格式 → ParsedDocument
│   │       ├── InferenceMetricsExtractor.java
│   │       ├── document/                    # 结构化文档模型
│   │       │   ├── ParsedDocument / CleanedDocument / DocumentBlock ...
│   │       │   └── DocumentTextRenderer.java
│   │       ├── chunking/                    # 五种分块实现
│   │       ├── retrieval/                   # 改写、RRF、文件名匹配、排序
│   │       ├── parser/                      # Local + DocForge 解析适配
│   │       ├── docforge/                    # DocForge HTTP 客户端与 DTO
│   │       ├── graph/                       # 图抽取模型与实体归一化
│   │       ├── minio/                       # 对象路径与存储
│   │       └── upload/                      # 异步任务进度上报
│   └── resources/
│       ├── application.yml                  # 主配置 + vector + rag
│       ├── application-ollama.yml
│       ├── application-datasource.yml
│       ├── application-local.yml            # 本地凭据（gitignore）
│       ├── static/index.html                # 主前端 UI
│       ├── static/eval.html                 # 评测前端 UI
│       ├── static/favicon.svg
│       └── db/*.sql                         # 表注释脚本
├── src/test/java/                           # 单元测试（当前 38 个 *Tests）
├── scripts/                                 # appctl.sh、start-all.sh
├── Makefile                                 # make restart-java/docforge/all
└── 待办优化方案.md                           # 本文档
```

### 0.3 核心数据流

#### 文件上传与向量化

```text
POST /api/files/upload/async（推荐，前端默认）
  → FileUploadTaskService.createTask()        # 落盘临时文件 + MySQL document_upload_task
  → FileUploadTaskWorker 轮询消费
  → DocumentParseService.parse()              # auto/local/docling/maker 路由
  → DocumentCleaningService.clean()
  → DocumentChunkingService.chunk()
  → VectorChunkBatchWriter 批量 EmbeddingModel.embed(List) + JDBC batch
  → DocumentCleaningLogService + DocumentStorageService（MySQL + MinIO）
  → 可选 GraphIndexTaskService 入队 Neo4j 构图

POST /api/files/upload（同步，兼容保留）
  → 同上流水线，占用 HTTP 请求线程直至完成
```

#### RAG 对话

```text
POST /api/chat (message, useRag, fileIds, systemPrompt)
  → QueryRewriteService（规则改写 + 意图分类）
  → RagRetrievalService.retrieve()
      ├── Dense 向量召回（pgvector）
      ├── BM25 召回（ParadeDB Jieba）
      └── RRF 融合 → RetrievalCandidateRanker（加权/去重/阈值/单文件限额）
  → FileEmbeddingService.buildRagPrompt()
  → ChatClient → Ollama qwen3:8b
  → TransactionLogService + RagCallLogService
  → 返回 reply + sources + ragStatus
```

### 0.4 数据库分工

| 库 | 表 | 用途 |
|----|-----|------|
| MySQL `myllm` | `transaction_log` | 每次对话的模型调用指标 |
| MySQL `myllm` | `rag_call_log` | RAG 检索模式、命中数、改写、耗时，关联 transaction_log_id |
| MySQL `myllm` | `document_cleaning_log` | 清洗前后文本、策略、字符统计，关联 file_id |
| MySQL `myllm` | `document_storage_log` | MinIO 路径与解析审计（模式/引擎/降级原因） |
| MySQL `myllm` | `document_upload_task` | 异步上传任务状态、进度、临时文件路径 |
| MySQL `myllm` | `document_graph_index_task` | Neo4j 构图/删除任务、重试与 DEAD 审计 |
| MySQL `myllm` | `harness_*` | Harness Run/Step/Tool/Event/Approval/Artifact 控制面 |
| MySQL `myllm` | `model_eval_*` | 本地模型评测套件、问题、Run、Item |
| PostgreSQL `myllm` | `file_embeddings` | 分块文本 + embedding vector（写入校验 1024 维，物理 `vector(1024)` 待迁移）+ JSONB 元数据 + BM25 索引 |
| MinIO `myllm` | `uploads/`、`parsed/` | 原文件与解析后 Markdown |
| Neo4j（可选） | Document/Section/Chunk/Entity | 结构图与实体关系（默认不参与在线 RAG） |

**关联键**：`file_id`（UUID）串联清洗日志与向量分块；`transaction_log.id` ← `rag_call_log.transaction_log_id`。

### 0.5 API 一览

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/chat/health` | 健康检查 |
| POST | `/api/chat` | 对话（支持 RAG、fileIds 过滤） |
| POST | `/api/files/upload` | 同步上传并向量化 |
| POST | `/api/files/upload/async` | 异步上传（202 Accepted，前端默认） |
| GET | `/api/files/tasks/{taskId}` | 上传任务状态与进度 |
| GET | `/api/files/tasks/{taskId}/result` | 上传任务完成结果 |
| POST | `/api/files/tasks/{taskId}/cancel` | 取消上传任务 |
| GET | `/api/files/parse-capabilities` | 解析引擎能力与扩展名白名单 |
| GET | `/api/files/parse-estimate` | 解析耗时预估 |
| GET | `/api/files` | 已入库文件列表 |
| GET | `/api/files/search` | 向量检索 |
| DELETE | `/api/files/{fileId}` | 删除文件全部分块 |
| GET | `/api/files/{fileId}/graph-status` | Neo4j 构图任务状态 |
| POST | `/api/files/{fileId}/graph-retry` | 手工重试图索引任务 |
| GET | `/api/graph/health` | Neo4j 健康检查 |
| POST | `/api/graph/backfill` | 历史文件构图回填 |
| GET | `/api/logs/requests` | 交易日志 + RAG 日志联查（支持 from/to/limit） |
| GET/POST/DELETE | `/api/eval/*` | 本地模型评测套件、问题、Run、报告管理 |
| GET/POST | `/api/harness/runs/*` | Harness Run/Step/Event/Cancel 控制面 API（默认关闭） |

### 0.6 已实现能力 vs 仍偏 Demo 的部分

| 能力 | 状态 | 说明 |
|------|------|------|
| 本地对话 | ✅ | Ollama qwen3:8b / OpenAI 兼容 |
| 文件上传向量化 | ✅ | 同步 + 异步双路径 |
| 异步上传任务 | ✅ | `FileUploadTaskWorker` + 进度上报 + 超时恢复 |
| 多格式解析 | ✅ | 本地 + DocForge Docling/Maker，auto 路由 |
| MinIO 原文件/解析结果存储 | ✅ | `document_storage_log` 审计 |
| 数据清理 v2 | ✅ | conservative-v2 |
| 多策略分块 v1 | ✅ | 5 种策略 + smart 路由 |
| RAG 检索对话 | ✅ | 改写 → 混合召回 → RRF → 排序 → 状态机 |
| 分块元数据 JSONB | ✅ | sourceFileName、chapterLocation、解析来源等 |
| embedding 含来源文件名 | ✅ | `EmbeddingInputComposer` |
| 混合检索 BM25 + 向量 | ✅ | ParadeDB pg_search + Dense + RRF |
| RAG 状态机 + 相似度阈值 | ✅ | `NO_HIT`/`LOW_RELEVANCE`/`APPLIED` 等 |
| Neo4j 结构图索引（可选） | ✅ | M0～M3，默认关闭，Shadow 模式 |
| Reranker 精排 | ❌ | 见「混合检索实施状态 → 步骤二」 |
| 文件列表/删除/按文件过滤检索 | ✅ | 删除联动 pgvector / MySQL / MinIO |
| 双日志（交易 + RAG） | ✅ | 含检索模式、BM25 命中、改写等字段 |
| 清洗审计持久化 | ✅ | MySQL |
| HNSW 向量索引 | ✅ | 启动时自动创建 cosine HNSW |
| 批量 embedding + JDBC batch | ✅ | `VectorChunkBatchWriter` |
| 跨存储补偿清理 | ✅ | 入库失败 `FileIngestionCleanupService` 回滚 |
| 删除链路补全 | ✅ | 联动 pgvector / 清洗日志 / 存储日志 / MinIO |
| Token 级分块 | ✅ | `BgeM3TokenEstimator` + `use-token-limits` |
| Spring AI 稳定版 | ✅ | 1.0.0 GA + 新 starter 坐标 |
| 根目录 README | ✅ | `README.md` |
| Harness 控制平面 | ✅/进行中 | H0～H3 完成；H4 Context/引用校验已完成第一阶段，Artifact 溢出待补 |
| 本地模型评测 | ✅/待增强 | `eval/` 已具备套件、Run、逐题评分和摘要；需接入 RAG 回归与 CI 阈值 |

### 0.7 已知技术债（来自近期联调）

- 当前混合检索库端口为 **5434**（Docker `rag_hybrid`），凭据 `pgvector/pgvector`；旧 `rag_test:5433` 仅保留为回退副本。
- `mvn spring-boot:run` 需 `-DsocksProxyHost=` 禁用 SOCKS，否则本机 PostgreSQL 连不上。
- 主数据源（MySQL）与向量数据源（PostgreSQL）分离，`VectorDataSourceConfig` 只暴露 `vectorJdbcTemplate`，避免覆盖 JPA 数据源。
- 前端默认分块策略为 **smart**，后端 `application.yml` 默认 **fixed**；上传时以前端传参为准。
- `GlobalExceptionHandler` 返回纯文本，与 Spring 默认 JSON 错误格式不一致。
- `FileEmbeddingService` 已改为批量 embedding + JDBC batch 入库。
- 删除文件时已联动 `document_cleaning_log`、`document_storage_log` 与 MinIO 对象。
- `pom.xml` 已升级 Spring AI `1.0.0`，但仍保留 `spring-milestones` 仓库配置，可清理以减少供应链噪声。
- 前端 `index.html` 约 1465 行、`eval.html` 约 887 行，JS/CSS 未拆分。
- 当前测试类约 38 个，Harness/Chunk/Retrieval 覆盖增加明显；仍缺 Controller 集成测试、真实 PostgreSQL/Neo4j/MinIO 组合链路与端到端 RAG 评测。
- 向量写入已做模型维度探测和批量写入校验，但 PostgreSQL 建表语句仍是 `embedding vector`，物理列尚未固定为 `vector(1024)`。
- `FileEmbeddingService.ensureVectorTableExists()` 在业务服务内自动建表/改表/建索引，投产前应迁移到可审计的版本化迁移脚本。

---

## 2026-07-05 代码结构复盘与代码级待优化项

> 本节基于当前工作区代码扫描整理，作为后续优化执行入口。它补充并纠偏下方历史待办，不替代已有专题章节。

### A. 当前代码结构快照

| 区域 | 现状 | 主要文件/包 | 评价 |
|------|------|-------------|------|
| HTTP API | 核心 4 个 Controller + Eval + Harness API | `controller/*`、`eval/controller`、`harness/api` | API 边界基本清晰，但错误响应格式不统一 |
| 对话/RAG | 单次 Chat 调用 + 规则改写 + Dense/BM25/Graph 编排 | `ChatService`、`RagRetrievalService`、`Bm25RetrievalService`、`support/retrieval` | 主链路可用，下一步重点是 reranker、上下文预算和评测闭环 |
| 文档入库 | 上传任务 → 解析 → 清洗 → 分块 → batch embedding → pgvector → 可选构图 | `FileEmbeddingService`、`FileUploadTask*`、`VectorChunkBatchWriter` | 功能完整，但 `FileEmbeddingService` 已承担过多职责 |
| 文档解析 | Local + DocForge Docling/Maker，支持同步/异步解析 | `DocumentParseService`、`support/parser`、`support/docforge` | 路由和审计较完整，仍需 IR 化和流式上传 |
| 数据清洗/分块 | conservative-v2 + fixed/recursive/document/semantic/smart | `DocumentCleaningService`、`DocumentChunkingService`、`support/chunking` | 已从字符硬切升级，后续做父子切片和结构粒度校准 |
| 向量存储 | PostgreSQL + pgvector + HNSW + ParadeDB BM25 | `FileEmbeddingService`、`VectorChunkBatchWriter`、`db/*.sql` | 已具备混合检索基础，但物理维度和版本化模型仍待补齐 |
| MinIO | 原文件与解析 Markdown 存储 | `DocumentStorageService`、`support/minio` | 审计具备，当前对象 Key 未含 `fileId/version`，同日同名会覆盖 |
| Neo4j | 可选结构图/实体图，默认 Shadow/关闭 | `GraphIndex*`、`Neo4j*`、`support/graph` | M0～M3 基础链路具备，生产化还缺 heartbeat/fencing 和图召回评测 |
| Harness | Run/Step/Tool/Event/验证闭环已成型 | `harness/*` | H4 第一阶段完成；还缺 Artifact 溢出、contentHash 校验和 Shadow Chat |
| Eval | 本地模型套件、Run、Item、评分摘要 | `eval/*`、`evals/*` | 已有雏形，需接入 RAG 检索指标与 CI 阈值 |
| 运维脚本 | 单服务启停、状态、日志 | `scripts/appctl.sh`、`Makefile` | 可用性提升明显，仍需 shellcheck/超时/平台兼容验证 |

当前最大文件与拆分信号：

| 文件 | 行数级别 | 风险 | 建议 |
|------|----------|------|------|
| `static/index.html` | 1400+ | UI、状态、请求和渲染混在一个文件 | 拆为 `api.js`、`upload.js`、`chat.js`、`styles.css` 或小型 SPA |
| `FileEmbeddingService` | 600+ | 解析、存储、补偿、建表、检索、Prompt 组装职责过多 | 拆为 IngestionPipeline、VectorSchemaManager、RagPromptAssembler |
| `DocForgeClient` | 400+ | 能力缓存、上传、同步/异步任务、错误映射耦合 | 拆为 CapabilityClient、ParseJobClient、ErrorMapper |
| `RagRetrievalService` | 400+ | 多路召回、诊断、降级、融合集中在一个类 | 引入 RetrievalPipeline/DiagnosticsBuilder |
| `GraphIndexingService` | 400+ | 结构图、实体图、批量写 Cypher 混合 | 拆结构图写入器、实体关系写入器、Cypher 批处理器 |
| `HarnessOrchestrator` | 300+ | Loop、Step、Tool、Repair、验证聚合 | 后续拆 ActionExecutor、RepairCoordinator |

### B. 当前核心处理逻辑

#### B.1 RAG 在线问答

```text
ChatController
  -> ChatService.chat()
      -> RagRetrievalService.retrieve()
          -> QueryRewriteService
          -> Dense: FileEmbeddingService.search()
          -> BM25: Bm25RetrievalService.search()
          -> Graph: GraphRetrievalPort.search()（默认 Shadow/关闭）
          -> ReciprocalRankFusion
          -> RetrievalCandidateRanker
      -> FileEmbeddingService.buildRagPrompt()
      -> ChatClient
      -> transaction_log + rag_call_log
```

已做得好的部分：

- RAG 状态已区分 `SKIPPED_BY_INTENT`、`NO_HIT`、`LOW_RELEVANCE`、`APPLIED`、`RETRIEVAL_FAILED`。
- Dense/BM25 采用 RRF 融合，不直接混合异构分数。
- 文件名线索、fileId 范围、单文档限额和内容哈希去重已进入排序链路。

主要待优化：

- 在线 Chat 的 RAG Prompt 仍按固定 TopK 拼接，没有总 Token 预算、相邻 Chunk 合并、引用白名单和不可信证据边界；Harness 中已做的 Context/Verification 还未回灌到 `/api/chat`。
- `rag_call_log` 仍缺 `raw_hit_count`、`accepted_hit_count`、`top_score`、`lowest_applied_score`、`context_token_count`、`rerank_*` 等诊断字段。
- Reranker 尚未接入，当前最终排序仍依赖融合分、相似度和规则加权。

#### B.2 文档入库与向量化

```text
FileEmbeddingController
  -> 同步 upload 或异步 upload/async
  -> FileUploadTaskWorker
  -> FileEmbeddingService.embedAndStoreWithProgress()
      -> DocumentParseService
      -> DocumentStorageService(MinIO)
      -> DocumentCleaningService
      -> DocumentChunkingService
      -> VectorChunkBatchWriter
      -> DocumentCleaningLogService
      -> GraphIndexTaskService.enqueueIndex()
```

已做得好的部分：

- 清洗日志延后到向量写入成功后保存，减少半失败脏数据。
- 向量写入已从逐条改为 `embed(List)` + JDBC batch。
- pgvector、MySQL 清洗/存储日志和 MinIO 删除链路已有补偿清理。

主要待优化：

- 当前没有 `document -> document_version -> chunk -> embedding_model_version` 四层模型，文件仍是“写入即在线”，不能做到原子激活、蓝绿切换和版本回滚。
- 建表/改表/建索引在 `FileEmbeddingService.ensureVectorTableExists()` 中运行，难以审计和灰度；应迁移到 SQL 版本化迁移。
- `embedding` 物理列仍是 `vector`，仅靠启动探测和写入校验保证 1024 维；应迁移为 `vector(1024)` 或新模型版本表。
- `DocForgeClient` 和 `MinioStorageService` 都使用 `file.getBytes()`，大文件会额外占用内存；应改成 Resource/InputStream 流式 multipart 和 MinIO streaming put。
- MinIO 对象 Key 仅按日期和文件名生成，同日同名上传会覆盖，应加入 `fileId`、版本号或内容哈希。

#### B.3 Worker 与跨存储一致性

当前有三套异步/任务机制：

| 机制 | 现状 | 风险 |
|------|------|------|
| 上传任务 | `document_upload_task` + `FileUploadTaskWorker` + `FOR UPDATE SKIP LOCKED` | 无 lease token/heartbeat/fencing；超时后旧 Worker 仍可能回写成功 |
| 构图任务 | `document_graph_index_task` + 指数退避 + DEAD | 有任务领取和重试，但无 heartbeat/fencing |
| Harness Run | `harness_run/step/event` + lease/heartbeat/fencing/预算/取消 | 可靠性模型更完整，但尚未统一复用到上传/构图 |

主要待优化：

- 将上传与构图任务逐步迁移到与 Harness 一致的 lease/fencing 模型。
- 长耗时解析、embedding、构图阶段增加协作式取消检查。
- 跨 MySQL/PostgreSQL/MinIO/Neo4j 的补偿步骤持久化，形成轻量 Saga，而不是仅靠进程内 best-effort。
- Worker 并发度、embedding 并发、DocForge 并发和 Neo4j 批量写入统一限流。

#### B.4 Harness Engineering

已完成：

- Run/Step/Tool/Event/Approval/Artifact 表与实体。
- Tool Registry、Tool Policy、Tool Executor 和四个只读 Tool。
- 应用控制 Tool Loop、Worker、Run API、取消与心跳。
- Context 预算、不可信证据包装、引用白名单、最终答案校验和最多两次 Repair。

主要待优化：

- 超大 Tool 输出写入 MinIO Artifact，Context 中只注入摘要和 Artifact 引用。
- citation 增加 `contentHash`，最终答案引用需校验向量库 Chunk 未被替换。
- Worker 恢复后从 Artifact 重建完整 Observation，而不是只恢复脱敏摘要。
- Shadow Chat 接入 `/api/chat`，先对比 Harness 答案和现有答案，不改变线上结果。
- `ToolExecutor` 当前 Tool 调用与审计更新仍较集中，后续可拆为短事务审计 + 事务外执行，降低阻塞。

### C. 代码待优化清单（按优先级）

#### P0：影响正确性/一致性/投产安全

| # | 优化项 | 当前证据 | 建议落点 |
|---|--------|----------|----------|
| P0-1 | 向量物理维度固定 | `CREATE TABLE` 仍为 `embedding vector NOT NULL` | 新建 `file_embeddings_bge_m3_v1` 或迁移为 `vector(1024)`，配套 HNSW 重建 |
| P0-2 | 文档版本化与原子激活 | 当前 `file_id + chunk_index` 直接入在线表 | 新增 document/version/chunk/embedding 四层模型，`ACTIVE` 后参与检索 |
| P0-3 | 在线 RAG Context 安全 | `buildRagPrompt()` 直接拼接 chunk 文本 | 新增 `RagPromptAssembler`：Token 预算、证据转义、引用 sourceId |
| P0-4 | 上传/构图 Worker fencing | 任务超时恢复后旧 Worker 仍可能回写 | 增加 `lease_owner/lease_token/lease_expires_at/heartbeat_at` |
| P0-5 | 流式文件传输 | `DocForgeClient`、`MinioStorageService` 使用 `getBytes()` | 改 InputStream/Resource 流式上传，保留大小限制 |
| P0-6 | MinIO 对象不可覆盖 | 对象 Key 仅日期 + 文件名 | Key 加 `fileId` 或 `documentVersionId` |
| P0-7 | RAG 诊断日志补齐 | `rag_call_log` 缺 top/accepted/context/rerank 字段 | 新增迁移脚本与 `RagCallLogService` 写入 |
| P0-8 | Schema 迁移治理 | 业务服务内自动 DDL | 收敛到 `src/main/resources/db`，评估 Flyway/Liquibase |

#### P1：检索质量与可验证性

| # | 优化项 | 当前证据 | 建议落点 |
|---|--------|----------|----------|
| P1-1 | Cross-Encoder Reranker | 仍未接入 reranker | `RagRetrievalService` 融合后 20～30 条 → rerank → final |
| P1-2 | RAG 评测集闭环 | `evals` 有数据雏形，未形成 CI 阈值 | Recall@5/10、MRR、错误引用率、NO_HIT 准确率 |
| P1-3 | 父子切片与相邻 Chunk 合并 | 当前只返回命中 Chunk | 子块召回、父块/邻居块补上下文 |
| P1-4 | LLM Query Rewrite v2 | 当前规则改写 | 低温度、超时降级、保留规则版兜底 |
| P1-5 | Graph RAG 三路融合 | Graph 默认 Shadow | Dense + BM25 + Graph N 路加权 RRF + Shadow 指标 |
| P1-6 | Docling/DocTags IR | 远程结果转 Markdown 再解析 | 保留页码、坐标、表格单元格、图片引用 |
| P1-7 | Harness Shadow Chat | Harness 未接入现有 `/api/chat` | 异步运行 Harness，对比答案、引用和耗时 |

#### P2：规模化、可观测与工程卫生

| # | 优化项 | 当前证据 | 建议落点 |
|---|--------|----------|----------|
| P2-1 | 全链路 traceId | 多存储日志缺统一关联 ID | Controller 入口生成 traceId，贯穿 MySQL/PG/MinIO/Neo4j/DocForge |
| P2-2 | Prometheus/Micrometer 指标 | 当前主要靠日志和数据库 | RAG P95、NO_HIT 比例、DocForge 降级率、队列深度 |
| P2-3 | Embedding/Query 缓存 | 每次重复内容仍可能重算 | `content_hash` 复用向量，短期 Query Embedding cache |
| P2-4 | 前端模块化 | `index.html`、`eval.html` 过大 | 拆分 JS/CSS，保留静态部署或迁移轻量 SPA |
| P2-5 | 大文件/大规模索引方案 | 当前 HNSW 默认 | 评估 IVFFlat、halfvec、分区、读副本 |
| P2-6 | 统一 JSON 错误响应 | 普通异常返回文本，Harness 返回 JSON | `{code,message,traceId,details}` |
| P2-7 | Maven/仓库清理 | `spring-milestones` 仍存在 | 删除不再需要的 milestone repo，锁定依赖版本 |
| P2-8 | 空目录与文档漂移清理 | 根目录存在空 `dto/`、`service/` 目录 | 人工确认后逐个删除，禁止批量删除 |

### D. 建议后续实施顺序

```text
1. 向量物理维度 + Schema 迁移治理
2. 在线 RAG Prompt 安全与 Token 预算
3. RAG 日志字段补齐 + 评测集基线
4. Reranker 精排
5. 上传/构图 Worker lease/fencing 统一
6. 文档版本化与原子激活
7. Harness Shadow Chat 与 Artifact 溢出
8. DocForge/MinIO 流式上传 + MinIO Key 版本化
9. Graph RAG Shadow 指标 → 三路融合灰度
10. 前端模块化、Prometheus、traceId 和 E2E
```

---

## 当前待办优先级速览（2026-07-05）

### 已完成（近期）

| 项 | 说明 |
|----|------|
| RAG 状态机 + 相似度阈值 | `NO_HIT` / `LOW_RELEVANCE` / `APPLIED` / `SKIPPED_BY_INTENT` 等，`min-similarity=0.45` |
| 分块元数据入库 | `metadata` JSONB + `heading_path` 等列；`ChunkMetadataEnricher` |
| 检索编排 v1.1 | 独立候选排序器：文件名加权→去重→双阈值→动态单文件限额 |
| 问题改写 v1（规则） | 预处理 + 意图分类 + 检索查询重写 |
| 入库文件名进 embedding | `EmbeddingInputComposer`，提升按书名检索命中率 |
| 混合检索代码链路 v1 | `Bm25RetrievalService` + RRF + 双通道准入 + VECTOR 降级 |
| DocForge 多模式解析 | `auto/local/docling/maker` 路由与解析审计 |
| 异步上传任务 | `POST /api/files/upload/async` + `FileUploadTaskWorker` + 进度上报 |
| Neo4j 结构图 M0～M3 | 构图任务表、幂等写入、超时恢复、回填与重试 API |
| Harness H0～H4 第一阶段 | 控制面、只读 Tool、Worker、Context 预算、引用校验与 Repair |
| 本地评测模块 | Eval 套件、问题、Run、逐题评分、摘要和评测页面 |
| 服务控制脚本 | `appctl.sh` 支持单服务 start/stop/restart/status/logs |

### P0 — 基础投产项（2026-07-03 已完成；2026-07-05 复盘新增 P0 见上方清单）

| # | 项 | 状态 |
|---|-----|------|
| 1 | **批量 embedding + JDBC batch** | ✅ `VectorChunkBatchWriter` |
| 2 | **pgvector HNSW 索引** | ✅ `vector.index.hnsw-*` + 启动自动建索引 |
| 3 | **跨存储原子性与补偿** | ✅ 入库失败调用 `FileIngestionCleanupService` |
| 4 | **删除链路补全** | ✅ DELETE 联动 MySQL + MinIO |
| 5 | **Token 级分块** | ✅ `structural-token-v1` + `BgeM3TokenEstimator` |
| 6 | **Spring AI 稳定版** | ✅ 1.0.0 + `spring-ai-starter-model-*` |
| 7 | **根目录 README** | ✅ `README.md` |

> 注意：本表记录 2026-07-03 已完成的基础优化；最新复盘发现的物理 `vector(1024)`、文档版本化、在线 RAG Context 安全、Worker fencing 等 P0 项已归档到「2026-07-05 代码结构复盘与代码级待优化项」。

### P1 — 检索与质量

| # | 项 | 说明 |
|---|-----|------|
| 8 | **Reranker 精排** | `bge-reranker-v2-m3`；粗排 20～30 条 → 精排 → final 4～6 |
| 9 | **混合检索评测集** | Recall@5、错误引用率基准（汽车手册 vs 贷记卡文档） |
| 10 | **问题改写 v2（LLM）** | 低温度 + 超时降级，提升内容问答召回 |
| 11 | **在线 RAG 上下文 Token 预算** | Harness 已完成 Context 预算；`/api/chat` 仍需非固定 `final-top-k=5` 的动态截断 |
| 12 | **Graph RAG 上线** | Shadow 评测后 Dense + BM25 + Graph 三路 RRF |
| 13 | **实体抽取启用** | 白名单类型 + 置信度过滤 + Chunk 证据链（当前默认关闭） |
| 14 | 相邻 chunk 去重与合并（Prompt 侧） | 检索侧已按 `content_hash` 去重 |
| 15 | 扩展 `rag_call_log` | `rawHitCount`、`topScore`、`rerank_duration_ms` 等待补字段 |

### P1 — 文档解析

| # | 项 | 说明 |
|---|-----|------|
| 16 | **Docling JSON/DocTags 统一 IR** | 当前远程结果转 Markdown 再解析，丢失表格单元格、页码、坐标 |
| 17 | **DocForgeClient 流式上传** | `file.getBytes()` 全量复制；改用 Resource/InputStream multipart |
| 18 | **解析路由配置化** | auto 策略硬编码；PDF 是否优先 Maker、超时预算可配置 |
| 19 | **MinIO 对象版本化** | 同日同名上传可能覆盖；对象名加入 `fileId` 或版本号 |
| 20 | DocForge 引擎真实状态 | `/v1/engines` 区分 enabled/installed/ready；Maker 回退 Docling 返回 `actual_engine` |

### P2 — 架构与规模化

| # | 项 | 说明 |
|---|-----|------|
| 21 | 文档版本化四层模型 | document → version → chunk → embedding，蓝绿切换 |
| 22 | Embedding 缓存 | 按 `content_hash` 复用向量，避免重复计算 |
| 23 | 任务 Worker lease/fencing + 并发度 | 上传/构图每次调度只处理 1 条；需 lease/heartbeat/fencing、可配置并发 + 限流 |
| 24 | 全链路 traceId | Java / DocForge / MinIO / MySQL / PostgreSQL 日志串联 |
| 25 | Prometheus 指标 | 解析成功率、RAG P95、队列深度、降级率 |
| 26 | 统一 JSON 错误响应 | `GlobalExceptionHandler` 改为 `{code, message, traceId}` |
| 27 | 前端模块化 | 拆分 `index.html` 为独立 JS/CSS 或 SPA |
| 28 | IVFFlat / halfvec 评估 | 数据量百万级后的索引方案 |
| 29 | 集成测试与 E2E | Controller 集成测试、Neo4j Testcontainers 全链路 |
| 30 | Embedding 服务熔断 | 超时、熔断和并发隔离 |

### 建议实施路线图

```text
阶段 1（稳定性）
  批量 embedding + HNSW 索引
  → 删除链路补全 + 跨存储补偿
  → Token 级分块

阶段 2（检索质量）
  Reranker 精排
  → 评测集固化 + 参数校准
  → LLM 问题改写 v2

阶段 3（规模化）
  文档版本化 + 原子激活
  → Graph RAG Shadow → 灰度上线
  → 可观测性（traceId + Prometheus）
```

---

## 混合检索实施状态与后续精排

> 背景：在汽车用户手册联调中，纯向量检索下无关大文档（如贷记卡说明书）仍可达 **0.52+** 相似度并进入 Top 5；文件名加权可缓解「按书名查」，但无法根治语义噪声。下一批建议按 **BM25 混合召回 → Reranker 精排** 推进。

### 步骤一：混合检索（BM25 + Dense 向量）

| 维度 | 内容 |
|------|------|
| **是什么** | BM25 为关键词稀疏检索；与 bge-m3 向量检索（Dense）并行召回，再用 RRF 等融合排名 |
| **解决什么问题** | 专有名词、型号、错误码、文档标题等字面匹配；降低「语义都像文档」导致的误排 |
| **已实现方案** | ParadeDB `pg_search` 真 BM25（Jieba 中文分词）+ pgvector Dense + RRF |
| **接入点** | `RagRetrievalService.retrieve()` 并行编排两路候选，融合后进入去重、文件限额和 Final TopK |
| **配置** | `rag.retrieval.hybrid-enabled`、`bm25-top-k`、`rrf-k`、`bm25-index-name` |
| **验收** | 问「查看《汽车用户手册（2025年版）》.docx」时贷记卡文档不进 final Top 5 |

待办 checklist：

- [x] 提供 `file_embeddings_bm25.sql`：`file_name` + `chunk_text` Jieba BM25 索引
- [x] 实现 BM25 查询、能力检测、TopK 召回和失败降级
- [x] 实现 Dense + BM25 的 RRF 融合，不直接混加异构原始分数
- [x] 融合结果接入现有文件名加权、去重、单文档限额流程
- [x] `rag_call_log` 增加检索模式、两路命中数、融合数、耗时和降级原因
- [x] 旁路迁移到 `rag_hybrid:5434`，创建 `pg_search 0.24.1` 中文 BM25 索引；保留旧容器回退
- [ ] 用汽车手册 + 贷记卡文档评测集对比融合前后 Recall@5

### 步骤二：Reranker 精排（Cross-Encoder）

| 维度 | 内容 |
|------|------|
| **是什么** | 对 `(query, chunk)` 成对输入 cross-encoder，输出比余弦相似度更准的相关性分数 |
| **解决什么问题** | 粗排候选中「分数接近但语义无关」的 chunk（如 0.54 vs 0.53） |
| **建议实现** | 本地 `bge-reranker-v2-m3`（Ollama/ONNX）或 Spring AI reranker 集成 |
| **接入点** | 混合召回 / 向量召回之后、`min-similarity` 过滤之前或之后（推荐：融合后取 20～30 条 → rerank → 取 final 4～6） |
| **配置草案** | `rag.retrieval.rerank-enabled`、`rerank-candidate-top-k`、`rerank-model` |
| **降级** | reranker 不可用时回退向量+加权排序，并记录 `fallbackReason` |
| **验收** | 相同评测集上错误引用率下降；记录 `rerank_duration_ms` |

待办 checklist：

- [ ] 选型并接入 reranker 模型（优先 `bge-reranker-v2-m3`）
- [ ] 扩展 `RagRetrievalService`：粗排 → rerank → final TopK
- [ ] `rag_call_log` 增加 `reranked_hit_count`、`rerank_duration_ms`、`rerank_model_version`
- [ ] reranker 超时/失败降级策略
- [ ] 对比启用前后 `APPLIED` 场景的引用准确率

### 建议实施顺序

```text
1. BM25 索引 + 混合召回（RRF）     ← 先扩大召回面、压字面噪声
2. Reranker 精排                  ← 再对融合候选做高精度排序
3. 评测集固化 + 参数校准           ← 统一调 candidate/final/threshold
```

---

# 待办优化方案（详细）

## RAG 切片与向量化生产化改造

记录日期：2026-07-01

### 实施记录：保守数据清理 v1（2026-07-01）

已完成：

- [x] 在文件读取后、切片前接入独立的 `DocumentCleaningService`。
- [x] 解析结果改为结构化 Block，支持标题、段落、列表项、表格行、代码块和引用块。
- [x] DOCX 按正文元素顺序读取段落与表格，并识别常见 Heading 样式和列表。
- [x] Markdown 识别标题、列表、引用、表格行和围栏代码块。
- [x] CSV 按表格行保留；TXT、JSON、XML、HTML、LOG 进入通用段落解析。
- [x] 实现 Unicode NFC、BOM/零宽字符/软连字符、控制字符和不间断空格清理。
- [x] 普通文本执行行尾及连续空白规范化，代码块保留内部空白。
- [x] 对长度不少于 20 字符的完全重复正文段落执行保守去重。
- [x] 增加清理后最小字符数、最大删除比例和乱码比例质量门禁。
- [x] 清理版本、字符数、重复块数量、规则命中数和警告进入清理报告及应用日志。
- [x] 文件上传响应增加清理版本、清理前后字符数和重复块数量。
- [x] 前端上传结果展示清理前后字符数。
- [x] 新增 3 个独立单元测试并通过；主代码 Java 17 编译通过。
- [x] 在本地 MySQL `myllm` 数据库创建 `document_cleaning_log` 表。
- [x] 持久化清洗前/后文本、Unicode 字符数、UTF-8 字节数、规则版本和重复块数量。
- [x] 使用 `file_id` 唯一关联向量文件，并建立文件 ID 与创建时间索引。
- [x] 本地数据库连接配置写入被忽略的 `application-local.yml`，避免凭据进入版本管理。

本次未完成、保留待办：

- [ ] 保存原始二进制文件；清理前后解析文本现已持久化，完整规则明细审计报告仍待实现。
- [ ] HTML 正文抽取以及 JSON、XML 的结构路径序列化。
- [ ] DOCX 页眉页脚、修订、批注和隐藏文本策略。
- [ ] 重复页眉页脚识别、近似去重和敏感信息脱敏。
- [ ] Token 切片、HNSW、批量入库已完成；物理 `vector(1024)`、文档版本化与原子激活仍按后续章节推进。

### 实施记录：多策略分块 v1（2026-07-01）

已完成：

- [x] 保留原有 `fixed` 固定大小分块，兼容已有 API 调用。
- [x] 新增 `recursive` 递归边界分块，按段落、换行、句末标点、次级标点、空格、硬切顺序降级，并支持重叠窗口。
- [x] 新增 `document` 文档结构分块，利用标题层级、段落、列表、表格、代码和引用 Block，向分块注入完整章节路径。
- [x] 新增 `semantic` 语义分块，按句子组成语义单元，批量生成 embedding，通过相邻余弦相似度的自适应分位点识别主题边界。
- [x] 语义分块增加最小/最大分块限制；语义单元不足时安全回退递归分块。
- [x] 新增 `smart` 智能分块：结构化文档路由到文档分块，长篇非结构化文档路由到语义分块，其余使用递归分块。
- [x] 智能分块的语义 embedding 异常时自动降级递归分块，并记录实际执行策略。
- [x] 上传 API 新增 `chunkStrategy` 参数，支持 `fixed/recursive/document/semantic/smart`。
- [x] 上传响应返回 `requestedChunkStrategy` 和 `appliedChunkStrategy`。
- [x] 页面上传区增加分块策略选择器，默认选择智能分块。
- [x] MySQL 清洗记录表增加请求策略、实际策略和分块数量。
- [x] pgvector 文件分块表增加请求策略和实际策略字段，已有表通过兼容 DDL 自动升级。
- [x] 新增 4 个分块单元测试；连同清理测试共 7 个测试全部通过。
- [x] 本地应用启动、MySQL Schema 更新、pgvector 表迁移及文件列表接口验证通过。

当前参数仍以字符数控制块大小，后续可在升级 Spring AI 后评估与 embedding 模型原生 tokenizer 对齐的 Token 上限。语义分块会额外执行一轮 embedding 用于检测主题边界，成本和耗时高于其他策略。

### 实施记录：清理与分块空白优化 v1（2026-07-01）

- [x] 清洗前审计文本不再为每个 Block 人为插入空行，统一使用单换行。
- [x] 新增上下文感知文档渲染器，按相邻 Block 类型选择单换行或空行。
- [x] 连续表格行、列表项、标题与正文使用单换行。
- [x] 仅较长正文段落和代码块边界保留一个空行。
- [x] 普通 Block 内部软换行转换为空格，代码块内部换行和缩进保持不变。
- [x] 文档结构分块器复用统一渲染规则，不再对每个 Block 固定添加双换行。
- [x] 所有分块策略结束后统一执行 Chunk 空白规范化，普通文本最多保留一个空行。
- [x] DOCX 标题识别增加 outline level、本地化标题样式名称和英文 Heading 样式支持。
- [x] 新增表格/列表紧凑渲染、Chunk 空白规范化和 DOCX outline 标题测试。
- [x] 清理与分块相关测试共 10 个，全部通过。
- [x] 优化前已停止应用；完成后未重新启动应用。

### 实施记录：RAG 检索编排 v1

记录日期：2026-07-01

**目标**：在纯向量 TopK 基础上，缓解按书名检索漏召、无关大文档误排。

**已完成**：

- [x] `RagRetrievalService`：candidate 召回 → `content_hash` 去重 → 文件名加权 → 单文档限额 → 阈值过滤 → final TopK
- [x] `QueryFilenameMatcher`：从查询提取 `《书名》` 等提示，对匹配 `source_file_name` 的 chunk 加分
- [x] `EmbeddingInputComposer`：入库时 `embedding_content` 前缀 `来源文件：{fileName}`
- [x] `application.yml`：`candidate-top-k=10`、`final-top-k=5`、`filename-boost=0.12`、`max-chunks-per-file=1`、`min-similarity=0.45`
- [x] `ChatService` 改为调用 `RagRetrievalService` 而非直接 `search()`

**已知局限（留给后续 BM25 + Reranker）**：

- 无关大文档在纯向量下仍可达 0.52+ 并进入 Top 5（如贷记卡说明书）
- 文件名加权仅对「按书名查」有效，无法根治语义噪声
- 粗排仍依赖 pgvector 余弦，无 cross-encoder 精排

### 实施记录：RAG 检索编排 v1.1

记录日期：2026-07-01

**本次优化**：

- [x] 新增独立 `RetrievalCandidateRanker`，将候选排序逻辑从服务中拆出并可单元测试。
- [x] 调整顺序为：文件名加权 → `content_hash` 去重 → 相关性过滤 → 单文档限额 → Final TopK。
- [x] 文件名加权提前到去重之前，避免相同内容跨文件时误删用户明确指定的来源。
- [x] 普通问答继续使用 `min-similarity=0.45`；明确文件范围使用 `filename-min-similarity=0.20`，但禁止只靠文件名加分挽救完全无关 Chunk。
- [x] 内容问答每个文件最多保留 2 个 Chunk；纯文件定位仍限制为 1 个。
- [x] `candidate-top-k` 从 10 提升到 30，并保证不小于 `final-top-k`。
- [x] 相似度和文件名加权配置增加边界保护。
- [x] 修复“查看胎压报警怎么处理”被误判为文件定位的问题。
- [x] 查询预处理增加连续空白归一化，文件名匹配增加全角括号归一化。
- [x] 相关离线测试共 30 个全部通过；未启动应用。

**v1.1 当时仍未实现**：BM25/稀疏并行召回、RRF 融合和 cross-encoder Reranker；其中 BM25 + RRF 已在下述 v1 完成，Reranker 仍待实施。

### 实施记录：混合检索 v1（BM25 + Dense + RRF）

记录日期：2026-07-01

**已完成**：

- [x] 新增 `Bm25RetrievalService`，通过 ParadeDB `pdb.score` 召回文件名和正文命中。
- [x] 新增 `ReciprocalRankFusion`，使用一基排名和 `rrf-k=60` 融合两路结果。
- [x] BM25 命中与达到阈值的 Dense 命中分别准入，避免用向量阈值误删 BM25-only 候选。
- [x] `pg_search` 或 BM25 索引不可用时自动回退为 VECTOR，并记录明确原因。
- [x] MySQL `rag_call_log` 已增加检索模式、Dense/BM25/融合命中数、BM25/RRF 耗时和降级原因。
- [x] 新增 ParadeDB 中文 Jieba BM25 索引脚本和 RRF/混合准入单元测试。
- [x] 明确文件名先解析为真实 `fileId`，Dense 与 BM25 同时限定范围，避免 2024/2025 版本互相污染。
- [x] 新建 `rag_hybrid:5434` 和独立数据卷，24 行数据及向量摘要与旧库完全一致。
- [x] 已创建 `file_embeddings_bm25_idx`；项目连接切换到 5434，旧 `rag_test:5433` 未删除。
- [x] 相关离线测试全部通过；未启动应用。

**部署结果**：ParadeDB PostgreSQL 18.4、`pg_search 0.24.1`、`vector 0.8.2` 均已验证；精确范围 BM25 查询只返回《汽车用户手册（2025年版）》目标分片。

### 实施记录：RAG 问题改写 v1（规则版）

记录日期：2026-07-01

**目标**：在向量检索前先规范化用户问题，提升按文件名定位与混合问法的检索稳定性。

**已完成**：

- [x] 新增 `QueryPreprocessor`：礼貌词清洗、标点归一化、文件名线索提取。
- [x] 新增 `QueryIntentClassifier`：识别 `FILENAME_LOOKUP`、`CONTENT_QA`、`HYBRID`、`SKIP_RAG`。
- [x] 新增 `QueryRewriteService`：基于意图生成检索查询（文件定位类优先用文件名提示词）。
- [x] `RagRetrievalService` 接入改写结果，并在 `SKIP_RAG` 场景直接跳过检索。
- [x] `ChatService` 增加改写日志（original/normalized/retrieval/intent/hints）。
- [x] `application.yml` 增加开关：`rag.query-rewrite.enabled`、`skip-rag-on-chitchat`。
- [x] 新增 `QueryRewriteServiceTests` 覆盖文件名定位、闲聊跳过、混合问法。

**下一步（问题改写 v2）**：

- [ ] 接入 LLM 改写（低温度、超时降级）以提升内容问答检索召回。
- [x] 扩展 `rag_call_log` 持久化 `rewritten_query`、`rewrite_strategy`、`query_intent`、`normalized_query`。
- [ ] 结合评测集对比改写前后 Recall@K 与错误引用率。

### 一、推荐基线方案

采用“结构感知切片 + Token 上限 + 句子级重叠 + 固定维度向量 + HNSW 索引 + 阈值过滤 + 候选重排 + 模型与切片版本化”。

初始建议参数：

```yaml
rag:
  chunk:
    strategy-version: structural-token-v1
    target-tokens: 450
    max-tokens: 600
    min-tokens: 80
    overlap-tokens: 75
    max-chunks-per-document: 10000
  retrieval:
    candidate-top-k: 30
    final-top-k: 6
    similarity-threshold: 由评测集确定
    max-context-tokens: 3500
    rerank-enabled: true
  embedding:
    model: bge-m3
    model-version: bge-m3-v1
    dimensions: 1024
  index:
    type: hnsw
    distance: cosine
    ef-search: 100
```

注意：以上参数是初始基线，投产前必须使用真实问题评测集校准。

### 二、方案对比

#### 2.1 切片方案

| 方案 | 优点 | 缺点 | 适用场景 | 结论 |
|---|---|---|---|---|
| 固定字符切片 | 实现简单、速度快 | 不等于 Token，容易截断语义 | Demo、小规模验证 | 不用于生产 |
| 固定 Token 切片 | 长度可控 | 仍可能截断句子和章节 | 普通纯文本 | 作为兜底 |
| 递归边界切片 | 优先保持章节、段落、句子完整 | 实现稍复杂 | 通用知识库 | 推荐 |
| 文档结构切片 | 保留标题、列表、表格、代码结构 | 需要按格式实现解析器 | 制度、手册、技术文档 | 强烈推荐 |
| 语义切片 | 可根据主题变化确定边界 | 成本高、结果不稳定、难调试 | 高价值小规模语料 | 暂不优先 |
| 父子切片 | 小块精准召回，大块补充上下文 | 数据模型和检索流程更复杂 | 长文档、章节型文档 | 第二阶段引入 |
| 滑动窗口 | 保持上下文连续 | 重复存储和重复召回较多 | 缺少明确结构的长文本 | 作为补充 |

推荐组合：

```text
文档结构识别
  -> 按章节/段落生成语义单元
  -> 超过 Token 上限时递归切分
  -> 在完整句子边界添加重叠
  -> 保存标题路径、位置、页码等元数据
```

#### 2.2 向量索引方案

| 方案 | 查询性能 | 召回率 | 写入/构建成本 | 内存 | 适用场景 |
|---|---|---|---|---|---|
| 精确搜索，无向量索引 | 数据增长后较慢 | 精确 | 低 | 低 | 小数据、召回基准 |
| HNSW | 快 | 通常较高 | 构建较慢 | 高 | 在线知识库，默认选择 |
| IVFFlat | 快 | 强依赖 lists/probes | 构建较快 | 较低 | 大批量导入、更新较少 |
| HNSW + halfvec | 快 | 有轻微精度损失 | 较复杂 | 较低 | 大规模、内存受限 |
| 二值量化 + 原向量重排 | 很快 | 第一阶段有损 | 复杂 | 很低 | 千万级以上数据 |

当前项目优先使用 HNSW；保留精确搜索作为召回质量基准。

### 三、第一阶段：投产必需项

#### 3.1 修正 RAG 状态机

- [x] 区分原始召回数与有效召回数（`ChatService` 使用 `rawHits` / `acceptedHits`）。
- [x] 检索结果为空或全部低于阈值时，设置 `ragApplied=false`。
- [x] 将 RAG 状态扩展为 `DISABLED` / `RETRIEVAL_FAILED` / `NO_HIT` / `LOW_RELEVANCE` / `APPLIED` / `SKIPPED_BY_INTENT`。
- [x] API 响应增加 `ragStatus`、`ragApplied`。
- [ ] 将模型生成状态与 RAG 检索状态在 `transaction_log` 中完全分离存储。
- [ ] 支持两种无命中策略可配置：严格拒答 vs 回退普通聊天（当前固定回退）。
- [ ] `rag_call_log` 补全 `raw_hit_count`、`accepted_hit_count`、`top_score`、`lowest_applied_score`。

#### 3.2 替换固定字符切片

- [x] 抽象 `DocumentParser`，解析结果统一为结构化 `ParsedDocument`。
- [x] 按文件格式分别解析 TXT、Markdown、DOCX、CSV、JSON、XML、HTML。
- [x] DOCX 补充标题级别、列表、表格内容解析。
- [x] Markdown 识别标题、代码块、表格和列表。
- [x] 使用与模型 Token 长度相关的切片上限（`BgeM3TokenEstimator` + `use-token-limits`）。
- [x] 中文分隔优先级：章节标题、空行、换行、句号/问号/叹号、分号、逗号、Token 硬切（递归/结构分块已覆盖主体逻辑）。
- [ ] 小于 `min-tokens` 的片段合并到相邻片段。
- [ ] 重叠内容优先选择完整句子，不机械截取字符。
- [ ] 重叠比例控制在约 10%～20%。
- [x] 保存 `headingPath`、`chunkIndex`、`tokenCount` 和内容哈希；页码仍依赖 Docling/DocTags IR 待补。
- [ ] 限制单文件最大 Chunk 数。

不同内容的建议起始参数：

| 内容类型 | 目标 Token | 重叠 Token | 特殊处理 |
|---|---:|---:|---|
| 普通说明文档 | 350～500 | 50～80 | 按标题、段落、句子切分 |
| 法规/制度 | 250～450 | 30～60 | 每条、每款尽量独立 |
| 技术手册 | 400～650 | 60～100 | 保留完整标题路径 |
| API 文档 | 250～500 | 30～60 | 每个接口或方法为独立单元 |
| 代码 | 200～500 | 少量 | 按类、方法、函数切分 |
| FAQ | 一问一答 | 通常不重叠 | 问题和答案不得拆开 |
| 表格 | 按表或行组 | 视情况 | 重复表头并保留列语义 |

#### 3.3 固定向量维度

- [ ] 当前 `bge-m3` 在配置和写入校验层固定为 1024 维，但 PostgreSQL 物理列仍为 `vector`，需迁移到 `vector(1024)` 或模型版本表。
- [x] 启动时调用一次 embedding 探测，验证实际维度等于 1024（`EmbeddingDimensionValidator`）。
- [x] 检查向量中的所有数值均为有限数值，拒绝 NaN 和 Infinity（启动探测 + 写入校验）。
- [x] 数据库记录 embedding 模型名称和模型修订版本；维度字段随版本化模型表补齐。
- [ ] 禁止不同维度或不同语义空间的模型共用一个向量列。
- [ ] 模型维度不匹配时禁止继续写入，不自动容错。

推荐采用“每个 embedding 模型版本一张向量表”：

```text
document_chunk_embedding_bge_m3_v1
document_chunk_embedding_new_model_v1
```

这样可以固定维度、独立建索引，并支持蓝绿切换和快速回滚。

#### 3.4 建立 HNSW 索引

- [x] 为当前 `embedding vector` 创建 cosine HNSW 索引（`ensureHnswIndex()` + `file_embeddings_hnsw.sql`）。
- [ ] 生产环境使用 `CREATE INDEX CONCURRENTLY`，降低对在线写入的阻塞。
- [ ] 为 `knowledge_base_id`、`document_id`、`document_version_id` 创建普通 B-tree 索引（待四层版本模型落地）。
- [x] 初始使用 `m=16`、`ef_construction=64`。
- [ ] 查询初始使用 `hnsw.ef_search=100`，通过压测调整。
- [ ] 使用 `EXPLAIN (ANALYZE, BUFFERS)` 验证查询实际使用索引。
- [ ] 定期将 HNSW 结果与精确搜索结果对比，监控近似召回率。

参考 SQL：

```sql
CREATE INDEX CONCURRENTLY idx_chunk_embedding_hnsw
ON document_chunk_embedding_bge_m3_v1
USING hnsw (embedding vector_cosine_ops)
WITH (m = 16, ef_construction = 64);
```

#### 3.5 完善过滤检索

- [ ] 查询限定文件时，关注 HNSW 先扫描再过滤导致返回结果不足的问题。
- [ ] pgvector 版本支持时启用 `hnsw.iterative_scan = strict_order`。
- [ ] 选中少量小文件时，评估在文件范围内使用精确搜索。
- [x] 全知识库检索默认使用 HNSW；需继续用 EXPLAIN 和评测监控确认实际计划与召回。
- [ ] 不为每个文件创建单独的 partial HNSW 索引。
- [ ] 多租户大规模场景按租户组或知识库分区，不按单个文件分区。

#### 3.6 原子化入库

- [ ] 文件上传只创建文档版本，初始状态为 `PENDING`。
- [ ] 后台执行解析、切片、批量向量化和写入。
- [ ] 所有 Chunk 完成并通过校验后，将文档版本切换为 `ACTIVE`。
- [ ] 任意片段失败时将版本标记为 `FAILED`，不能让部分片段参与检索。
- [ ] 任务具备幂等性，可按 `documentVersionId + chunkIndex` 安全重试。
- [ ] 记录失败阶段、错误原因、重试次数和最后重试时间。
- [ ] 增加文件大小、解析耗时、Chunk 数量和任务执行时间限制。

### 四、推荐数据模型

建议拆分为四层：

```text
document
  -> document_version
      -> document_chunk
          -> document_chunk_embedding_<model_version>
```

核心字段：

```text
document
  id, knowledge_base_id, file_name, content_type, created_at

document_version
  id, document_id, content_hash, parser_version,
  chunk_strategy_version, status, error_message,
  created_at, activated_at

document_chunk
  id, document_version_id, parent_chunk_id, chunk_index,
  heading_path, content, embedding_content, token_count,
  content_hash, metadata, created_at

document_chunk_embedding_bge_m3_v1
  chunk_id, knowledge_base_id, document_id,
  document_version_id, embedding vector(1024), created_at
```

约束要求：

- [ ] `(document_version_id, chunk_index)` 唯一。
- [ ] 同一内容通过 `content_hash` 去重或复用向量。
- [ ] 只有 `ACTIVE` 文档版本允许参与检索。
- [ ] 文档版本激活应使用数据库事务完成。
- [ ] 表名、Schema 名必须使用白名单配置，不接受请求参数拼接。

### 五、第二阶段：检索质量提升

#### 5.1 相似度阈值

- [x] 初次向量召回扩大到 Top 30（`candidate-top-k=30`）。
- [ ] 使用评测集确定 similarity threshold，不直接凭经验写死。
- [ ] 记录最高相似度、最低采用相似度、原始命中数和阈值后命中数到 `rag_call_log`。
- [x] 阈值后无结果时进入 `NO_HIT` 或 `LOW_RELEVANCE`（`ragApplied=false`）。

#### 5.2 去重与上下文合并

- [x] 使用 `contentHash` 去除相同内容（`RetrievalCandidateRanker`）。
- [ ] 合并同一文档内连续或重叠的相邻 Chunk（Prompt 侧）。
- [x] 限制单个文档最多贡献的上下文数量（`max-chunks-per-file=2`）。
- [ ] 按总上下文 Token 预算截断，而不是仅按 Chunk 数截断。
- [x] 向模型提供文件名、章节路径（`buildRagPrompt` + `RagSource`）。

#### 5.3 父子切片

- [ ] 子块建议 250～450 Token，用于生成向量和精准召回。
- [ ] 父块建议 1000～1600 Token，用于向 LLM 提供完整上下文。
- [ ] 子块命中后回溯父块，合并重复父块。
- [ ] 父块过大时改为子块及其前后相邻块，避免撑满上下文。

#### 5.4 Reranker

> **状态**：规划中，详见文档顶部「后续检索优化 → 步骤二」。

- [ ] 将向量召回的 20～50 个候选交给 cross-encoder 重排。
- [ ] 最终保留 4～8 个 Chunk。
- [ ] 记录 reranker 模型版本、耗时和重排分数。
- [ ] reranker 不可用时允许降级为原始向量排序，并记录降级原因。

### 六、第三阶段：规模化能力

#### 6.1 混合检索

> **状态**：代码和 ParadeDB/pg_search 部署已完成，规模化效果评测待完成。

- [x] 增加 ParadeDB BM25 检索服务、能力检测和 Dense 降级。
- [x] Dense 和 BM25 结果使用 RRF 合并。
- [ ] 对错误码、型号、人名、专有名词等查询提高关键词检索权重。
- [ ] 混合候选统一进入 reranker。

#### 6.2 大规模索引方案

- [ ] HNSW 内存无法接受时评估 IVFFlat。
- [ ] IVFFlat 在已有代表性数据后创建索引。
- [ ] 100 万行以内可从 `lists ≈ rows / 1000` 开始测试。
- [ ] 超过 100 万行可从 `lists ≈ sqrt(rows)` 开始测试。
- [ ] 查询从 `probes ≈ sqrt(lists)` 开始测试。
- [ ] 更大规模时评估 halfvec、二值量化及原向量二次重排。
- [ ] 需要横向扩展时评估读副本、分区或分片方案。

#### 6.3 缓存与异步任务

- [ ] 按标准化文本哈希缓存 embedding 结果。
- [ ] 短时间重复问题缓存 Query Embedding。
- [x] 文件向量化异步任务（`document_upload_task` + `FileUploadTaskWorker` + 超时恢复）。
- [x] 批量调用 embedding（入库路径 `VectorChunkBatchWriter`、semantic 分块均已支持 batch）。
- [ ] 为 embedding 服务设置超时、熔断和并发隔离。
- [ ] 上传/构图 Worker 可配置并发度（当前每次调度仅处理 1 条）。

### 七、模型和切片版本迁移

使用蓝绿迁移，不覆盖当前在线向量：

1. [ ] 注册新 embedding 模型及固定维度。
2. [ ] 创建新版本向量表。
3. [ ] 从原始文档重新解析和切片。
4. [ ] 后台批量生成新向量。
5. [ ] 校验 Chunk 数、向量维度、失败数量和内容覆盖率。
6. [ ] 创建新表 HNSW 索引。
7. [ ] 对同一评测集运行新旧版本对比。
8. [ ] 灰度部分查询到新版本。
9. [ ] 新版本切换为 `ACTIVE`。
10. [ ] 旧版本保留回滚窗口。
11. [ ] 稳定后由人工决定是否逐个清理旧文件或旧表，不执行批量删除。

### 八、日志与可观测性

RAG 调用日志建议增加：

```text
rag_status
retrieval_attempted
raw_hit_count
threshold_hit_count
reranked_hit_count
applied_chunk_count
similarity_threshold
top_score
lowest_applied_score
query_embedding_duration_ms
retrieval_duration_ms
rerank_duration_ms
embedding_model
embedding_model_version
chunk_strategy_version
fallback_reason
```

监控项：

- [ ] Query Embedding P50/P95/P99。
- [ ] 向量检索 P50/P95/P99。
- [ ] reranker P50/P95/P99。
- [ ] RAG 总耗时与模型生成耗时分别统计。
- [ ] `NO_HIT` 比例。
- [ ] `RETRIEVAL_FAILED` 和 fallback 比例。
- [ ] 平均候选数、采用 Chunk 数和上下文 Token 数。
- [ ] 文件解析、切片、向量化失败率。
- [ ] 部分入库可见数量，目标必须为 0。
- [ ] HNSW 与精确搜索 Recall 差异。
- [ ] PostgreSQL 慢查询、缓存命中率和索引体积。

### 九、投产验收

#### 9.1 离线评测

- [ ] 建立至少 100～500 条真实业务问题评测集。
- [ ] 每个问题标注相关文档、章节或 Chunk。
- [ ] 统计 `Recall@5`、`Recall@10`、MRR、`nDCG@10`。
- [ ] 统计 `NO_HIT` 准确率和错误引用率。
- [ ] 对比不同切片大小、重叠、阈值和索引参数。
- [ ] 对比是否启用父子切片、混合检索和 reranker。

#### 9.2 初始验收目标

```text
向量检索 P95 < 200ms
RAG 检索流程 P95 < 500ms，不含 LLM 生成
文件版本部分可见率 = 0
近似索引 Recall@10 >= 95%
失败任务可重试且不会重复写入
embedding 模型或维度不匹配时禁止写入
```

延迟目标需要根据生产机器配置、向量规模和并发量最终校准。

### 十、实施优先级汇总

#### P0：上线前必须完成

- [x] 修复 RAG 空结果仍标记成功的问题（`NO_HIT` / `LOW_RELEVANCE` + `ragApplied=false`）。
- [ ] 固定物理 `vector(1024)`；启动时校验模型实际维度已完成（`EmbeddingDimensionValidator`）。
- [x] 增加 HNSW cosine 索引和 `file_id` 等过滤字段普通索引。
- [x] 替换固定字符切片，实现 Token 感知切片（`structural-token-v1`）。
- [x] 增加相似度阈值（`min-similarity=0.45`）。
- [ ] 增加在线 RAG 上下文 Token 预算（Harness Context 预算已完成，`/api/chat` 仍待接入）。
- [x] 增加模型版本、切片版本和内容哈希字段。
- [ ] 文件入库采用版本化、原子激活和幂等重试。
- [x] 异步上传任务（大文件不再阻塞 HTTP）。
- [x] 批量 embedding + JDBC batch 入库。
- [x] 删除链路联动 MySQL 清洗/存储日志与 MinIO。
- [x] 跨 MinIO/MySQL/PostgreSQL 失败补偿机制（`FileIngestionCleanupService`）。
- [ ] 增加上传大小、Chunk 数量、TopK 和超时限制。
- [ ] 建立基础评测集和核心监控。
- [x] 根目录 README 与 Spring AI 稳定版升级。

#### P1：质量优化

- [x] 候选扩大到 Top 20～50（当前 `candidate-top-k=30`，`final-top-k=5`）。
- [ ] 相邻 Chunk 合并；内容哈希去重已在检索排序侧完成。
- [ ] 父子切片。
- [x] **混合检索 BM25 + 向量**：代码、pg_search、中文索引、数据迁移和连接切换均已完成。
- [ ] **cross-encoder reranker**（见「后续检索优化 → 步骤二」）。
- [ ] 基于评测集校准参数。

#### P2：规模优化

- [x] 异步上传任务（`FileUploadTaskWorker`）。
- [x] 批量 embedding（`VectorChunkBatchWriter`）。
- [ ] Embedding 缓存、限流与熔断。
- [ ] IVFFlat、halfvec 或二值量化评估。
- [ ] 多模型蓝绿迁移、读副本和分区方案。
- [ ] 全链路 traceId + Prometheus 指标。
- [ ] 前端模块化与统一 JSON 错误响应。
- [ ] Controller 集成测试与 E2E RAG 评测。

### DocForge 多引擎解析（2026-07-02）

详细分析见 [`项目结构与DocForge多模式解析分析.md`](项目结构与DocForge多模式解析分析.md)。

已完成：

- [x] 统一 `auto/local/docling/maker` 四种模式，`marker` 作为兼容别名。
- [x] PDF 自动执行 Maker → Docling 降级，本地支持格式优先 Local。
- [x] 页面按单引擎能力控制扩展名和选项，不再把 Docling 格式暴露给 Maker。
- [x] 上传响应、MySQL 存储日志和 Chunk JSONB 记录请求模式、实际模式、引擎和降级原因。
- [x] 引擎能力缓存增加 TTL，支持 DocForge 新版引擎状态字段。

后续优化：

- [ ] DocForge `/v1/engines` 返回 enabled/installed/ready 和不可用原因。
- [ ] Maker 内部回退 Docling 时返回真实 `actual_engine` 与 `fallback_reason`。
- [x] Java 上传链路任务化（`POST /api/files/upload/async` + Worker，前端已切换）。
- [x] 批量 embedding、JDBC batch 和跨 MinIO/MySQL/PostgreSQL best-effort 补偿机制已完成；文档版本化 Saga 仍待实现。
- [ ] Docling JSON/DocTags 统一中间表示，保留页码、坐标、表格单元格和图片引用。
- [ ] 建立 Docling/Maker 解析质量、耗时和降级率评测集。
- [ ] DocForgeClient 流式 multipart 上传，避免 `file.getBytes()` 全量复制。
- [ ] 解析 auto 路由配置化（PDF 优先引擎、超时预算）。
- [ ] MinIO 对象名加入 `fileId` 或版本号，避免同名覆盖。

### Neo4j 知识图谱集成（2026-07-03）

详细方案见 [`Neo4j集成详细配置与开发计划.md`](Neo4j集成详细配置与开发计划.md)。

已完成 M0～M3 基础链路：

- [x] 接入官方 Java Driver，增加连接、数据库、健康检查和 fail-soft 配置。
- [x] 本地 Neo4j 5.26.28 创建 Document/Section/Chunk/Entity 约束及索引，状态均为 ONLINE。
- [x] MySQL 增加 `document_graph_index_task` 持久化构图任务表。
- [x] 实现 Document/Section/Chunk/NEXT 结构图的参数化批量幂等写入。
- [x] 上传后创建 INDEX 任务；删除后取消待执行索引并创建 DELETE 任务。
- [x] 实现任务领取、超时恢复、指数退避、DEAD 审计和手工重试接口。
- [x] 增加带数量上限的历史 pgvector 文件构图任务回填接口。
- [x] 启停和服务检查脚本支持 `myllm-neo4j`。

后续质量与投产项：

- [ ] 将运行容器从浮动 `neo4j:5` 标签固定为 `neo4j:5.26.28`，重建前人工确认卷备份。
- [ ] 完成一次真实文件上传到 Neo4j 结构图的端到端验收。
- [ ] 增加任务执行心跳和管理页面状态展示。
- [ ] 实现白名单实体/关系结构化抽取、置信度过滤与 Chunk 证据链。
- [ ] Graph Shadow 召回和 Graph 命中日志，不改变当前在线排序。
- [ ] 将双路 RRF 重构为 Dense + BM25 + Graph N 路加权 RRF。
- [ ] 完成离线评测、灰度、Reranker、备份恢复和故障演练后再启用在线 Graph RAG。

### Harness Engineering 改造（2026-07-04）

详细方案见 [`HarnessEngineering改造方案.md`](HarnessEngineering改造方案.md)。

核心方向：

- [x] 建立仓库级知识地图、执行计划、机械架构约束和一键验证入口（H0）。
- [x] 新增持久化 Harness Run/Step/Tool/Event/Approval/Artifact 控制平面。
- [x] 增加 lease、heartbeat、fencing、预算、取消和状态转换守卫（Run 路径已验证）。
- [x] 接入 RAG、文件列表、上传状态、构图状态等只读 Tool。
- [x] 使用应用控制的 Tool Loop，不允许模型绕过策略直接执行工具。
- [x] 增加 Harness Run 创建/查询/步骤/事件/取消 API，长耗时模型与 Tool 调用期间独立续租。
- [x] 增加 Context/Observation 预算和 `UNTRUSTED_EVIDENCE` Prompt Injection 边界。
- [x] 增加实际命中 sourceId 引用白名单、最终答案校验和最多两次 Repair。
- [ ] 将超大 Tool 输出写入 MinIO Artifact，并增加 citation contentHash、限定文件范围与关键结论覆盖校验。
- [ ] 通过 Shadow 灰度接入现有 `/api/chat`，首期不改变在线答案。
- [ ] WRITE/DESTRUCTIVE/EXTERNAL 工具默认关闭，后续通过参数哈希和人工审批开放。
- [ ] 将上传与构图 Worker 逐步统一到可靠任务租约和持久化补偿能力。
- [ ] 建立 Replay、Tool Contract、Testcontainers、E2E 和 Live Eval 分层评测。

### 实施记录：P0 投产优化（2026-07-03）

**已完成**：

- [x] `VectorChunkBatchWriter`：批量 `embed(List)` + JDBC `batchUpdate`，配置 `rag.embedding.batch-size=32`。
- [x] pgvector HNSW cosine 索引：启动时 `ensureHnswIndex()`，脚本 `db/file_embeddings_hnsw.sql`。
- [x] `EmbeddingDimensionValidator`：启动探测 embedding 维度与有限数值。
- [x] `FileIngestionCleanupService`：入库失败补偿清理；`deleteByFileId` 联动 pgvector / 清洗日志 / 存储日志 / MinIO。
- [x] Token 级分块：`BgeM3TokenEstimator` + `use-token-limits` + `structural-token-v1`。
- [x] Spring AI 升级至 1.0.0 GA，starter 改为 `spring-ai-starter-model-ollama/openai`。
- [x] 根目录 `README.md`。
- [x] 清洗日志改为向量写入成功后再持久化，降低半失败脏数据。

**仍属 P2（未在本轮实现）**：

- [ ] 完整文档版本四层模型（document → version → chunk → embedding）与原子激活。

### 实施记录：异步上传任务 v1（2026-07-03）

**目标**：大文件解析与向量化不再阻塞 Tomcat HTTP 线程。

**已完成**：

- [x] `POST /api/files/upload/async` 创建任务并返回 202 Accepted。
- [x] MySQL `document_upload_task` 持久化任务状态、阶段、进度与临时文件路径。
- [x] `FileUploadTaskWorker` 轮询消费，支持超时恢复与过期任务清理。
- [x] `FileEmbeddingService.embedAndStoreWithProgress()` 上报解析/分块/向量化进度。
- [x] 前端 `index.html` 默认走异步上传并轮询 `/api/files/tasks/{taskId}`。
- [x] 任务取消接口 `POST /api/files/tasks/{taskId}/cancel`。

**仍待优化**：

- [x] 入库路径批量 embedding + JDBC batch（`VectorChunkBatchWriter`）。
- [ ] Worker 可配置并发度与 embedding 限流。
- [x] 跨 MinIO/MySQL/PostgreSQL 失败补偿（`FileIngestionCleanupService`）；文档版本原子激活仍待 P2。

### 十一、架构亮点（审阅记录，2026-07-03）

以下设计值得保留，后续优化应在现有分层上增量演进：

1. **解析抽象良好**：`DocumentParser` + `ParsedDocument` 统一下游，引擎可插拔。
2. **RAG 编排成熟**：改写 → 混合召回 → RRF → 排序 → 状态机，逻辑分层清晰。
3. **Fail-soft 设计**：BM25 不可用降级向量、Neo4j 默认关闭、图任务失败不回滚向量。
4. **审计较完整**：解析模式、清洗、检索诊断均有持久化字段。
5. **异步任务基础设施**：上传与构图均有 MySQL 任务表 + Worker + 超时恢复。

### 十二、参考资料

- [Spring AI ETL Pipeline](https://docs.spring.io/spring-ai/reference/api/etl-pipeline.html)
- [Spring AI Retrieval Augmented Generation](https://docs.spring.io/spring-ai/reference/api/retrieval-augmented-generation.html)
- [Spring AI PgVector](https://docs.spring.io/spring-ai/reference/api/vectordbs/pgvector.html)
- [pgvector 官方文档](https://github.com/pgvector/pgvector/blob/master/README.md)
- [BAAI/bge-m3 模型说明](https://huggingface.co/BAAI/bge-m3)
